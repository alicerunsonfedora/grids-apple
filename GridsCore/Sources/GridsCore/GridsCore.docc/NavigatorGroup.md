# ``GridsCore/NavigatorGroup``

Use the navigator group to display items plainly, or when the presented picker styles
aren't enough.

@Image(source: "navigatorgroup", alt: "The navigator pane items in Xcode")
